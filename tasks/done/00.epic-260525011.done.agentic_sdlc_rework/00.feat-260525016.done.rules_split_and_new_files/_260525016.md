---
id: '260525016'
type: feat
status: done
priority: '00'
slug: rules_split_and_new_files
title: Split task-management rules; add QA and doc-maintenance rule files
---

# Split task-management rules; add QA and doc-maintenance rule files


## Problem statement

`rules/task-management.md` mixes composition concerns (frontmatter, body sections, summary template) and persistence concerns (lifecycle, CLI, validation). Persistence rules duplicate what `taskman.sh help` could authoritatively own. The new Planner, QA, and docs-sync roles need dedicated rule files.

## Objective

Rule files reorganized so each loadable rule serves exactly one role. Persistence centralized in `taskman.sh help`.

## Acceptance criteria

- `rules/task-composition.md` exists; contains body shapes, frontmatter, completion-summary template (composition only).
- `rules/qa.md` exists; contains QA mindset, role separation, validation principles.
- `rules/doc-maintenance.md` exists; contains what to check on closure (drift in `docs/architecture.md`, `docs/spec.md`, `docs/requirements.md`).
- Persistence rules removed from rule files; equivalent content lives in `taskman.sh help` output.
- `./plugins/agn/scripts/taskman.sh validate` passes on existing task tree after the split.
- README import block updated to reference the new rule file set.

## Scope

In scope: split `task-management.md`; author the two new rule files; migrate persistence content into `taskman.sh help`.

Out of scope: wiring the new files into specific skills or agents (covered by sibling features).

## Tasks

- `split_task_management_rules` (will be re-filed and linked to this feature)
- `create_qa_rules`
- `create_doc_maintenance_rules`
- `migrate_persistence_to_taskman_help`

## Summary

### Steps completed

1. Decomposed into 5 tasks (the 4 listed above plus `update_rule_import_blocks`, added as the final synchronization step).
2. Executed in low-risk-first order to protect the live session's rule context:
   - **`create_qa_rules`** — new `rules/qa.md` (~55 lines): QA mindset, role separation, what to check, severity, scope decisions, output shape.
   - **`create_doc_maintenance_rules`** — new `rules/doc-maintenance.md` (~35 lines): what to check, dependency order, when to propose, how to propose, explicit no-op note.
   - **`migrate_persistence_to_taskman_help`** — rewrote `taskman.sh usage()` to embed COMMANDS + PERSISTENCE MODEL sections; `help` now 102 lines.
   - **`split_task_management_rules`** — composed `rules/task-composition.md` (~95 lines) with composition-only content; deleted `rules/task-management.md`.
   - **`update_rule_import_blocks`** — swapped `task-management.md` references across CLAUDE.md, plugins/agn/README.md, docs/agn-specification.md; expanded import example to the 5-file set; updated "What is in flight" sections.

### Changes made

Created:
- `plugins/agn/rules/qa.md`
- `plugins/agn/rules/doc-maintenance.md`
- `plugins/agn/rules/task-composition.md`

Modified:
- `plugins/agn/scripts/taskman.sh` (usage function rewritten)
- `CLAUDE.md`, `plugins/agn/README.md`, `docs/agn-specification.md`

Deleted:
- `plugins/agn/rules/task-management.md`

Final rule set (5 files): `first-principles.md`, `task-composition.md`, `writing-guideline.md`, `qa.md`, `doc-maintenance.md`. Persistence reference at `./plugins/agn/scripts/taskman.sh help`.

### Notable decisions or deviations

- **Added a 5th task (`update_rule_import_blocks`)** beyond the 4 originally listed. The acceptance criterion "README import block updated to reference the new rule file set" required dedicated execution; without it, the import example would silently point at a deleted file.
- **Executed in low-risk-first order** (rules authoring → persistence migration → split + deletion → import-block sync) rather than the listed order. Rationale: the live session loads rules from `.claude/rules/` (symlinked to `plugins/agn/rules/`). Deleting `task-management.md` before its content is preserved elsewhere risked losing rule context mid-session.
- **`Migration`, `Approval`, `Relationship to Git` sections dropped from task-composition.md.** All three are persistence concerns; they now live in `taskman.sh help`. The split is clean — nothing duplicated, nothing lost.
- **Persistence section in `taskman.sh help` includes the `## Summary` precondition** rather than placing it in composition. Rationale: failing `feature close` because no summary exists is a lifecycle precondition; the reader hitting that error needs the rule next to the lifecycle commands, not in a separate composition doc.

### Risk surfaced

`taskman.sh` still lacks a command for transitioning epics/features from `backlog` to `active`. This gap surfaced in the previous feature (`unified_skills_and_cleanup`) and is not addressed by this feature either. It is now arguably the next-most-pressing taskman gap; worth a backlog task or rolling into a future feature.

### Links

- Tasks (all in `tasks/done/20260525_*.md`): `create_qa_rules`, `create_doc_maintenance_rules`, `migrate_persistence_to_taskman_help`, `split_task_management_rules`, `update_rule_import_blocks`
- Rule files: `plugins/agn/rules/{first-principles,task-composition,writing-guideline,qa,doc-maintenance}.md`
- Persistence reference: `./plugins/agn/scripts/taskman.sh help`
- Parent epic: `tasks/epics/20260525_agentic_sdlc_rework.md`
