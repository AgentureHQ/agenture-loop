---
id: '260525005'
type: task
status: done
priority: '00'
slug: delete_old_lifecycle_skills
title: Delete old lifecycle skills (final rename step)
---

# Delete old lifecycle skills (final rename step)


## Problem statement

Once new lifecycle skills are built and documentation references them exclusively, the 10 old skill directories are dead weight. Final step in the rename.

## Scope

In scope: delete 10 skill directories: `product-define`, `product-design`, `epic-create`, `feature-create`, `task-create`, `epic-implement`, `feature-implement`, `task-implement`, `qa-integration`, `qa-system`.

Out of scope: tool skills (`code-review`, `code-comment`, `code-commit`) — unchanged.

## Acceptance criteria

- The 10 listed skill directories under `plugins/agn/skills/` do not exist.
- The 5 new lifecycle skills (`define`, `design`, `plan`, `implement`, `validate`) exist.
- The 3 tool skills (`code-review`, `code-comment`, `code-commit`) exist.
- No other directories under `plugins/agn/skills/`.

## Quality gates

- `./plugins/agn/scripts/taskman.sh validate` exits 0.
- After deletion + `/reload-plugins`, `/agn:` tab completion in Claude Code shows only the 5 new lifecycle skills plus the 3 tool skills.

## Summary

### Steps completed

1. Deleted all 10 old skill directories in one `rm -rf` invocation.
2. Listed `plugins/agn/skills/` — confirmed exactly 8 directories remain (`code-comment`, `code-commit`, `code-review`, `define`, `design`, `implement`, `plan`, `validate`).
3. Ran `taskman.sh validate` — passed.
4. Confirmed via the system-reminder skill manifest that Claude Code now sees only the 5 new lifecycle skills + 3 tool skills; the 10 old skill names are gone.

### Changes made

Deleted 10 directories:
- `plugins/agn/skills/product-define/`
- `plugins/agn/skills/product-design/`
- `plugins/agn/skills/epic-create/`
- `plugins/agn/skills/feature-create/`
- `plugins/agn/skills/task-create/`
- `plugins/agn/skills/epic-implement/`
- `plugins/agn/skills/feature-implement/`
- `plugins/agn/skills/task-implement/`
- `plugins/agn/skills/qa-integration/`
- `plugins/agn/skills/qa-system/`

### Notable decisions or deviations

None. Mechanical deletion.

### Links

- `plugins/agn/skills/` (post-deletion state)
